services:
  redis:
    image: redis:7-alpine
    container_name: task-manager-redis
    ports:
      - "6380:6379"
    restart: unless-stopped
    volumes:
      - redis_data:/data

volumes:
  redis_data: